version: '3.8'
services:
  middle:
    image: mysql:8.0
    cap_add:
      - SYS_NICE
    restart: always
    environment:
      - MYSQL_DATABASE=springweb
      - MYSQL_ROOT_PASSWORD=123
    ports:
      - '3307:3306'



volumes:
  db:
    driver: local
